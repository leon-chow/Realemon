<project basedir=".">
    <target name="clean">
        <delete file="Main.class"/>
        <delete file="Character.class"/>
        <delete file="Combat.class"/>
    </target>

    <target name="compile">
        <javac srcdir="." destdir="."/>
    </target>

    <target name="run">
        <java classname="realmon/Main" fork="true"/>
    </target>

</project>